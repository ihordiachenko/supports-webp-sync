{
  "name": "supports-webp-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run build-lib && vite",
    "build-lib": "cd .. && npm run build && cd supports-webp-app",
    "build": "npm run build-lib && vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.2.2",
    "@vue/compiler-sfc": "^3.0.5",
    "vite": "^2.2.3"
  }
}
